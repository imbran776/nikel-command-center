PACKAGE: mining-command-fms-source.tar.gz
Contains React+TS source under src/, public assets, package.json.
Does NOT include node_modules or .env secrets.
Install: tar -xzf mining-command-fms-source.tar.gz && npm install && npm run build
